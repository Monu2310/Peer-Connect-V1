# Example env file for local development (DO NOT COMMIT REAL SECRETS)
# Copy this file to .env and fill in real values. .env is ignored by git.

# Mongo
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.x5w3hhy.mongodb.net/peerconnect?retryWrites=true&w=majority&appName=Cluster0

# JWT secret for signing app tokens
JWT_SECRET=change_me_to_a_strong_random_value

# App
PORT=5111
NODE_ENV=development

# Firebase Admin (use values from your service account JSON)
FIREBASE_PROJECT_ID=peerconnect-f6a5c
FIREBASE_CLIENT_EMAIL=your-service-account-email@peerconnect-f6a5c.iam.gserviceaccount.com
# IMPORTANT: keep the \n sequences exactly as shown when pasting from JSON
FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nLINE1\nLINE2\nLINE3\n-----END PRIVATE KEY-----\n
